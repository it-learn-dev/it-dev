

<div class="col-2 hidden-xs fixed-top" style="top:65px;">
    <div class="row">
      <div class="list-group" style="width:100%;">
      	<% if (categories.length > 0) { %>
	        <% for(var i = 0; i < categories.length; i++) { %>          
	            <a href="/cat/<%= categories[i].slug %>" class="list-group-item list-group-item-action "> <%= categories[i].name%></a>
	        <% } %>
	        <% }else{ %>
	        	<div class="list-group-item list-group-item-action">No categories</div>
	        <% } %>
      </div>
    </div>
</div>